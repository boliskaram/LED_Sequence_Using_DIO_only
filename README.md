# Sprints
# LED Sequence Using DIO only

### The system controls the LEDs using a button, each push turns on one LED until all LEDs are turned on, then each press turns off one LED until all LEDs are turned off, and the process is repeated.